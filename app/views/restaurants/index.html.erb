<h1>Restaurantes</h1>

<% @restaurants.each do |restaurant| %>
  <%= link_to restaurant.name, restaurant_path(restaurant) %>
  <p><%= restaurant.category %></p>
<% end %>
<li><%= link_to "New Restaurant", new_restaurant_path, class: "btn btn-primary" %></li>
